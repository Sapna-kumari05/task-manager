# Required on Railway (Variables tab)
JWT_SECRET=replace_with_a_long_random_secret
NODE_ENV=production

# Optional: persist SQLite across deploys (mount a Railway volume at /data)
DATABASE_PATH=/data/database.sqlite
